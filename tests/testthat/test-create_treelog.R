test_that("use", {
  skip("#99")
  expect_silent(
    create_treelog(
      #trees_filename = "",
      #log_every = "",
      #mode = "",
      #sort = "",
      #sanitize_headers = TRUE
    )
  )
})
